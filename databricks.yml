# This is a Databricks asset bundle definition for databricks-terraform.
# The Databricks extension requires databricks.yml configuration file.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.

bundle:
  name: databricks-terraform

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-345008256263057.17.azuredatabricks.net

  ## Optionally, there could be 'staging' or 'prod' targets here.
  #
  # prod:
  #   workspace:
  #     host: https://adb-1521681165526931.11.azuredatabricks.net
