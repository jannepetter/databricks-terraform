resources:
  jobs:
    my_job_serverless:
      name: "my_job_serverless"
      tasks:
        - task_key: test_task
          notebook_task:
            notebook_path: "../notebooks/test_and_config.py"
          environment_key: serverless_env

      environments:
        - environment_key: serverless_env
          spec:
            environment_version : "1"
            dependencies:
              - "${var.wheel_path}"

